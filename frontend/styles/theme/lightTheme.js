import { createTheme } from '@mui/material/styles';

const lightTheme = createTheme({
    palette: {
        mode: 'light',
    },
    colors: {
        primary: 'blue',
    }
});

export default lightTheme;